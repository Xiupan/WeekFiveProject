<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hangman: {{difficulty}}</title>
    <link rel="stylesheet" href="/css/master.css">
    <link href="https://fonts.googleapis.com/css?family=Nunito|Questrial" rel="stylesheet">
  </head>
  <body>
    <div class="header">
      <div class="header-container">
        <h2>Let's Play Hangman!</h2>
        <h2 id="difficulty">{{difficulty}} Mode</h2>
      </div>
      <div class="guess-number-container">
        <h2>You have {{guessesRemaining}} guesses left.</h2>
      </div>
    </div>
    <div class="main-area-container">
      <div class="generated-word-container">
        <h2>{{#hiddenWord}}{{.}} {{/hiddenWord}}</h2>
        <h2>{{#error}}{{error}}{{/error}}</h2>
      </div>
      <form class="user-guess-form" action="/main-scene" method="post">
        <input type="hidden" name="difficulty" value="{{difficulty}}">
        <input type="text" name="userGuessField" value="" autofocus>
        <button type="submit" name="submitButton">Make a Guess</button>
      </form>
      <div class="letters-guessed-container">
        <h3>Letters Guessed: {{#lettersGuessed}}{{.}} {{/lettersGuessed}}</h3>
      </div>
    </div>
  </body>
</html>
